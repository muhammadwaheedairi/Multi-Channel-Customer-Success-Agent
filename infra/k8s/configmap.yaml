apiVersion: v1
kind: ConfigMap
metadata:
  name: fte-config
  namespace: fte
data:
  ENVIRONMENT: "production"
  POSTGRES_HOST: "postgres-service"
  POSTGRES_DB: "fte_db"
  KAFKA_BOOTSTRAP_SERVERS: "kafka-service:9092"
  LOG_LEVEL: "INFO"
